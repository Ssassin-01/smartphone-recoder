name: smartphone_recorder
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_overlay_window: ^0.5.0
  flutter_riverpod: ^3.3.1
  flutter_screen_recording: ^2.0.25
  gal: ^2.3.2
  path_provider: ^2.1.5
  permission_handler: ^12.0.1
  share_plus: ^13.1.0
  video_player: ^2.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
